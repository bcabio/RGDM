const scribble = require('./../../scribbletune/src');

module.exports.generatePercussionLine = function generatePercussionLine() {
	let percussionClip = scribble.clip({
		notes: ['c4'],
		pattern: 
	})
}